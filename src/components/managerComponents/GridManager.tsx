import React from 'react';
import GridComponent from '../gridComponent/GridComponent';

const GridManager: React.FC = () => {
  return (
    <div>
      <h2>Gerenciamento de Grid</h2>
      <GridComponent />
    </div>
  );
};

export default GridManager;
